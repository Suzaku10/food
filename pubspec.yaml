name: food
description: MSIG Test

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  bloc: ^7.2.1
  cupertino_icons: ^1.0.2
  dartz: ^0.10.0
  data_connection_checker: ^0.3.4
  dio: ^4.0.1
  equatable: ^2.0.3
  flutter_bloc: ^7.3.1
  flutter_modular: ^4.2.0
  moor: ^4.6.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
